/**
 * VoteController
 *
 * @description :: Server-side logic for managing votes
 * @help        :: See http://sailsjs.org/#!/documentation/concepts/Controllers
 */

module.exports = {
	create: function(req, res) {
        // Grab tweet from req
        // 
    }
};

function parseTweet(req, res, next) {
    // Reject if there isn't exactly one candidate mentioned

    // Candidate.findByHandle(...)
}

function parseMentions(tweet) {

}

